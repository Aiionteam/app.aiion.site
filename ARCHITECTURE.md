# AIION 마이크로서비스 아키텍처 가이드

## 📋 목차
1. [전체 아키텍처 개요](#전체-아키텍처-개요)
2. [각 컴포넌트 역할](#각-컴포넌트-역할)
3. [서비스 간 통신 흐름](#서비스-간-통신-흐름)
4. [Gateway와 Eureka 통신 메커니즘](#gateway와-eureka-통신-메커니즘)
5. [동시성 및 충돌 방지 메커니즘](#동시성-및-충돌-방지-메커니즘)
6. [실제 요청 시나리오](#실제-요청-시나리오)
7. [포트 맵](#포트-맵)

---

## 전체 아키텍처 개요

```
┌─────────────────────────────────────────────────────────────┐
│                    Client Layer                             │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐  │
│  │         Frontend (Next.js 16 + React 19)             │  │
│  │                  Port: 3000                           │  │
│  │                                                       │  │
│  │  ┌───────────────────────────────────────────────┐   │  │
│  │  │         Presentation Layer                    │   │  │
│  │  │                                               │   │  │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  │   │  │
│  │  │  │  Atoms   │  │ Molecules│  │Organisms │  │   │  │
│  │  │  │          │  │          │  │          │  │   │  │
│  │  │  │ Button   │  │  Badge   │  │ Sidebar  │  │   │  │
│  │  │  │ Input    │  │  Card    │  │ Calendar │  │   │  │
│  │  │  │ Icon     │  │  Message │  │  Diary   │  │   │  │
│  │  │  └──────────┘  └──────────┘  └──────────┘  │   │  │
│  │  │                                               │   │  │
│  │  │  ┌───────────────────────────────────────┐  │   │  │
│  │  │  │         Templates                     │  │   │  │
│  │  │  │         MainLayout                    │  │   │  │
│  │  │  └───────────────────────────────────────┘  │   │  │
│  │  └───────────────────────────────────────────────┘   │  │
│  │                                                       │  │
│  │  ┌───────────────────────────────────────────────┐   │  │
│  │  │         State Management Layer                │   │  │
│  │  │                                               │   │  │
│  │  │  ┌──────────────────┐  ┌──────────────────┐ │   │  │
│  │  │  │   Zustand Store  │  │  React Query     │ │   │  │
│  │  │  │                  │  │                  │ │   │  │
│  │  │  │ - UI State       │  │ - Server State   │ │   │  │
│  │  │  │ - Local State    │  │ - Cache          │ │   │  │
│  │  │  │ - Slices:        │  │ - Auto Refetch   │ │   │  │
│  │  │  │   • ui           │  │ - Background Sync│ │   │  │
│  │  │  │   • diary        │  │                  │ │   │  │
│  │  │  │   • calendar     │  │                  │ │   │  │
│  │  │  │   • account      │  │                  │ │   │  │
│  │  │  │   • culture      │  │                  │ │   │  │
│  │  │  │   • health       │  │                  │ │   │  │
│  │  │  │   • path         │  │                  │ │   │  │
│  │  │  │   • soccer       │  │                  │ │   │  │
│  │  │  │   • interaction  │  │                  │ │   │  │
│  │  │  │   • avatar       │  │                  │ │   │  │
│  │  │  └──────────────────┘  └──────────────────┘ │   │  │
│  │  └───────────────────────────────────────────────┘   │  │
│  │                                                       │  │
│  │  ┌───────────────────────────────────────────────┐   │  │
│  │  │         API Layer                            │   │  │
│  │  │                                               │   │  │
│  │  │  ┌───────────────────────────────────────┐  │   │  │
│  │  │  │      API Client                       │  │   │  │
│  │  │  │  - fetchWithRetry                     │  │   │  │
│  │  │  │  - fetchFromGateway                   │  │   │  │
│  │  │  │  - Auto Retry (2회)                   │  │   │  │
│  │  │  │  - Timeout (30초)                     │  │   │  │
│  │  │  └───────────────────────────────────────┘  │   │  │
│  │  │                                               │   │  │
│  │  │  ┌───────────────────────────────────────┐  │   │  │
│  │  │  │      Endpoints                        │  │   │  │
│  │  │  │  - Gateway: http://gateway:8080      │  │   │  │
│  │  │  │  - Service Routes                     │  │   │  │
│  │  │  └───────────────────────────────────────┘  │   │  │
│  │  └───────────────────────────────────────────────┘   │  │
│  │                                                       │  │
│  │  ┌───────────────────────────────────────────────┐   │  │
│  │  │         App Router (Next.js 16)              │   │  │
│  │  │                                               │   │  │
│  │  │  - layout.tsx (Providers: React Query)      │   │  │
│  │  │  - page.tsx                                  │   │  │
│  │  │  - pages/HomePage.tsx                        │   │  │
│  │  │  - hooks/useHomePage.ts                      │   │  │
│  │  └───────────────────────────────────────────────┘   │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      │ HTTP 요청 (via API Client)
                      │ React Query → API Client → Gateway
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                    Edge Layer                               │
│                                                             │
│            Spring Cloud Gateway (WebFlux)                   │
│                    Port: 8080                               │
│              - API 라우팅                                    │
│              - Rate Limiting (준비됨)                        │
│              - CORS 처리                                     │
└──────────┬──────────────────────────────────┬───────────────┘
           │                                  │
           │ Eureka에 서비스 위치 조회         │
           ▼                                  │
┌─────────────────────────────────────────┐  │
│     Infrastructure Layer (독립)         │  │
│                                         │  │
│  ┌──────────────┐  ┌─────────────────┐ │  │
│  │   Eureka     │  │  Config Server  │ │  │
│  │   :8761      │  │     :8888       │ │  │
│  │              │  │                 │ │  │
│  │ 서비스 레지스트리│  │   중앙 설정 관리  │ │  │
│  └──────────────┘  └─────────────────┘ │  │
│         ▲                               │  │
│         │ 모든 서비스가 자신을 등록        │  │
└─────────┼───────────────────────────────┘  │
          │                                  │
          │                                  │ 실제 요청 전달
          │                                  ▼
┌─────────┴────────────────────────────────────────────────────┐
│                   Service Layer                              │
│            (모든 서비스가 Eureka 클라이언트)                   │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────┐  │
│  │  User    │  │  Diary   │  │ Calendar │  │   Soccer   │  │
│  │ Service  │  │ Service  │  │ Service  │  │  Service   │  │
│  │  :8082   │  │  :8083   │  │  :8084   │  │   :8085    │  │
│  └──────────┘  └──────────┘  └──────────┘  └────────────┘  │
│                                                              │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐                 │
│  │ Culture  │  │Healthcare│  │  Common   │                 │
│  │ Service  │  │ Service  │  │  Service  │                 │
│  │  :8086   │  │  :8087   │  │   :8081   │                 │
│  └──────────┘  └──────────┘  └───────────┘                 │
│                                                              │
│  ┌────────────────────────────────────┐                     │
│  │      AI Middleware (준비)          │                     │
│  │          :8090                     │                     │
│  │  - NLP, 추천, 감정 분석            │                     │
│  └────────────────────────────────────┘                     │
└──────────────────┬───────────────────────────────────────────┘
                   │
                   │ 데이터 접근
                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    Data Layer                               │
│                                                             │
│  ┌────────────────┐          ┌──────────────┐              │
│  │  PostgreSQL    │          │    Redis     │              │
│  │    :5432       │          │    :6379     │              │
│  │                │          │              │              │
│  │ - OLTP         │          │ - Cache      │              │
│  │ - 트랜잭션 DB   │          │ - Session    │              │
│  └────────────────┘          │ - Pub/Sub    │              │
│                              │ - Queue      │              │
│                              └──────────────┘              │
└─────────────────────────────────────────────────────────────┘
```

---

## 각 컴포넌트 역할

### 0. Frontend (Next.js + React)
**Client Layer**

#### 기술 스택
- **Next.js 16**: App Router 기반 SSR/SSG 프레임워크
- **React 19**: 최신 React 버전
- **TypeScript**: 타입 안정성
- **Tailwind CSS 4**: 유틸리티 기반 스타일링

#### 상태 관리
- **Zustand**: 클라이언트 상태 관리 (UI 상태, 로컬 상태)
  - 슬라이스 패턴으로 도메인별 분리
  - 10개 슬라이스: ui, diary, calendar, account, culture, health, path, soccer, interaction, avatar
- **React Query (@tanstack/react-query)**: 서버 상태 관리
  - 자동 캐싱 및 백그라운드 동기화
  - staleTime: 1분
  - refetchOnWindowFocus: false

#### 컴포넌트 구조 (Atomic Design)
```
atoms/          # 기본 UI 요소
  - Button, Input, Icon, Badge, Toggle, BackButton

molecules/      # 복합 UI 요소
  - CategoryBadge, ChatMessage, EventCard

organisms/      # 복잡한 기능 컴포넌트
  - Sidebar, CalendarView, DiaryView, AccountView
  - CultureView, HealthView, PathfinderView
  - ChatContainer, PromptInput, AvatarMode

templates/      # 페이지 레이아웃
  - MainLayout
```

#### API 통신
- **API Client** (`lib/api/client.ts`):
  - `fetchWithRetry`: 자동 재시도 (최대 2회)
  - `fetchFromGateway`: Gateway를 통한 백엔드 호출
  - Timeout: 30초
  - 5xx 에러 및 네트워크 에러 자동 재시도

#### 데이터 흐름
```
Component
  ↓ useQuery (React Query)
  ↓ API Client
  ↓ Gateway (8080)
  ↓ Microservice
```

#### 주요 페이지
- `HomePage`: 메인 페이지 (카테고리별 뷰 전환)
- `layout.tsx`: 루트 레이아웃 (Providers 포함)

#### 포트
- **3000**: 개발 서버
- **프로덕션**: 빌드된 정적 파일 또는 Node.js 서버

---

### 1. Eureka Server (Service Registry)
**독립적인 인프라 서비스**

- **역할**: 모든 마이크로서비스의 "전화번호부"
- **기능**:
  - 서비스들이 시작할 때 자신의 위치(IP, 포트)를 등록
  - 다른 서비스가 특정 서비스를 찾을 때 위치 정보 제공
  - Health Check를 통해 서비스 상태 모니터링
- **포트**: 8761
- **관계**: Gateway와 모든 서비스가 Eureka의 "클라이언트"

```yaml
# 등록된 서비스 예시
Eureka Registry:
  - gateway-server    → localhost:8080
  - user-service      → localhost:8082
  - diary-service     → localhost:8083
  - calendar-service  → localhost:8084
  - ai-middleware     → localhost:8090 (예정)
```

### 2. Config Server (중앙 설정 관리)
**독립적인 인프라 서비스**

- **역할**: 모든 서비스의 설정 파일을 중앙에서 관리
- **기능**:
  - application.yml 등 설정 파일 통합 관리
  - 환경별 설정 분리 (dev, prod 등)
  - 런타임에 설정 변경 가능
- **포트**: 8888
- **관계**: 모든 서비스가 시작 시 Config Server에서 설정을 가져감

### 3. Spring Cloud Gateway
**Edge Layer (API Gateway)**

- **역할**: 모든 외부 요청의 "관문"이자 "트래픽 경찰"
- **기능**:
  - API 라우팅: 요청을 적절한 마이크로서비스로 전달
  - Load Balancing: 여러 인스턴스 중 하나를 선택
  - CORS 처리
  - Rate Limiting (Redis 연동, 준비됨)
  - 인증/인가 (예정)
- **포트**: 8080
- **관계**: 
  - Eureka의 **클라이언트** (서비스 위치를 조회함)
  - 모든 마이크로서비스의 **프록시**

```yaml
# Gateway 라우팅 설정 예시
/user/**     → user-service:8082
/diary/**    → diary-service:8083
/calendar/** → calendar-service:8084
/ai/**       → ai-middleware:8090 (예정)
```

### 4. Microservices (Domain Services)
**독립적인 비즈니스 로직 서비스들**

- **공통 특징**:
  - 각각 독립적인 DB 스키마 사용
  - Eureka에 자신을 등록 (Eureka 클라이언트)
  - Config Server에서 설정 로드
  - Redis 캐싱 사용 가능
- **서비스 목록**:
  - common-service (8081): 공통 기능
  - user-service (8082): 사용자 관리
  - diary-service (8083): 일기 관리
  - calendar-service (8084): 일정 관리
  - soccer-service (8085): 축구 정보
  - culture-service (8086): 문화 정보
  - healthcare-service (8087): 건강 관리

### 5. AI Middleware (준비 중)
**AI/ML 전용 서비스**

- **역할**: AI 모델 추론 서비스
- **기능**:
  - NLP (자연어 처리)
  - 감정 분석
  - 추천 시스템
  - Redis 큐를 통한 비동기 처리
- **포트**: 8090
- **특징**: GPU 서버에 독립적으로 배포 가능

### 6. Data Layer

#### PostgreSQL
- **역할**: 메인 트랜잭션 DB
- **포트**: 5432
- **용도**: 모든 서비스의 영속 데이터 저장

#### Redis
- **역할**: 캐싱, 세션, Pub/Sub, 큐
- **포트**: 6379
- **용도**:
  - 캐시: 자주 조회되는 데이터
  - 세션: 사용자 세션 관리
  - Pub/Sub: 서비스 간 이벤트 전달
  - Queue: 비동기 작업 처리

---

## 서비스 간 통신 흐름

### Gateway가 서비스를 찾는 과정

```
1. Gateway 시작
   ↓
2. Eureka에 자신을 등록
   - 이름: gateway-server
   - 주소: localhost:8080
   ↓
3. 요청 받음: GET /user/123
   ↓
4. 라우팅 규칙 확인
   - /user/** → user-service로 전달
   ↓
5. Eureka에 질의
   - "user-service의 주소를 알려줘"
   ↓
6. Eureka 응답
   - "user-service는 localhost:8082에 있어"
   ↓
7. 실제 호출
   - http://localhost:8082/user/123
   ↓
8. User Service 응답
   ↓
9. Gateway가 Frontend에 응답 전달
```

### 마이크로서비스 시작 과정

```
1. 서비스 시작 (예: user-service)
   ↓
2. Config Server에서 설정 로드
   - http://config-server:8888/user-service
   ↓
3. 설정 적용 (DB 연결, Redis 설정 등)
   ↓
4. Eureka에 자신을 등록
   - 이름: user-service
   - 주소: localhost:8082
   - Health Check URL: /actuator/health
   ↓
5. 서비스 준비 완료
   ↓
6. 30초마다 Eureka에 Heartbeat 전송
   - "나 아직 살아있어!"
```

---

## Gateway와 Eureka 통신 메커니즘

### 🎯 베스트 프랙티스: 캐싱 + 직접 호출

**Gateway는 Eureka를 "통하지 않고" 서비스에 직접 요청합니다!**

```
✅ 올바른 방식 (우리의 구조):
Frontend → Gateway → Service (직접!)
             ↑
             │ (최초 1회 + 30초마다 갱신)
           Eureka

❌ 잘못된 방식 (안티패턴):
Frontend → Gateway → Eureka → Service
```

### 왜 이게 베스트 프랙티스인가?

#### 1. 성능 최적화 ⚡
```
매 요청마다 Eureka 조회 (X):
  요청 → Eureka 조회(10ms) → 서비스 호출(100ms) = 총 110ms
  1000 TPS = Eureka에 10초/초 부하

캐싱 + 직접 호출 (O):
  요청 → 메모리 조회(0.1ms) → 서비스 호출(100ms) = 총 100ms
  1000 TPS = Eureka에 0.033 TPS 부하 (30초마다 1번)
  
→ Eureka 부하 30,000배 감소! 🚀
```

#### 2. 장애 격리 🛡️
```
Eureka 다운 시:

매 요청마다 조회하는 방식:
  - Eureka 죽음 → 모든 요청 실패 💥
  
캐싱 방식:
  - Eureka 죽음 → 캐시된 주소로 계속 작동 ✅
  - 기존 서비스는 정상 운영
  - 새 서비스 등록만 안 됨
```

#### 3. 확장성 📈
```
서비스 인스턴스 증가 시:

캐싱 방식:
  1. user-service 인스턴스 2개 → 5개 증가
  2. 새 인스턴스들이 Eureka에 등록
  3. 30초 후 Gateway가 자동 인식
  4. 5개 인스턴스에 자동 부하 분산
  
→ 동적 스케일링 완벽 지원!
```

### Gateway의 실제 동작 흐름

#### 최초 1회 (Gateway 시작 시)
```
[Gateway 시작]
  ↓
[Eureka에 전체 서비스 목록 요청]
  GET http://eureka:8761/eureka/apps
  ↓
[응답 받음]
  {
    "user-service": ["192.168.1.10:8082", "192.168.1.11:8082"],
    "diary-service": ["192.168.1.20:8083"],
    "calendar-service": ["192.168.1.30:8084"]
  }
  ↓
[메모리에 캐싱]
  serviceCache = {
    "user-service": ["192.168.1.10:8082", "192.168.1.11:8082"],
    "diary-service": ["192.168.1.20:8083"],
    ...
  }
```

#### 이후 모든 요청 (캐시 사용)
```
[Frontend 요청]
  GET http://gateway:8080/user/123
  ↓
[Gateway - 라우팅 규칙 확인]
  /user/** → user-service
  ↓
[Gateway - 로컬 캐시 조회] ⚡ (0.1ms)
  serviceCache.get("user-service")
  → ["192.168.1.10:8082", "192.168.1.11:8082"]
  ↓
[Gateway - Client-Side Load Balancing]
  Round Robin 알고리즘
  → 192.168.1.10:8082 선택
  ↓
[Gateway - 직접 HTTP 호출] 🚀
  http://192.168.1.10:8082/user/123
  (Eureka를 거치지 않음!)
  ↓
[User Service 응답]
  ↓
[Gateway → Frontend 응답 전달]
```

#### 백그라운드 갱신 (30초마다)
```
[타이머 트리거]
  ↓
[Eureka에 변경사항만 조회]
  GET http://eureka:8761/eureka/apps/delta
  ↓
[변경사항 적용]
  - 새 서비스 추가
  - 죽은 서비스 제거
  - 인스턴스 수 변경
  ↓
[로컬 캐시 업데이트]
```

### Spring Cloud의 Client-Side Load Balancing

```java
// Gateway 내부 동작 (Spring Cloud LoadBalancer)
@Configuration
public class GatewayConfig {
    
    @Bean
    public RouteLocator customRouteLocator(RouteLocatorBuilder builder) {
        return builder.routes()
            .route("user-service", r -> r
                .path("/user/**")
                .uri("lb://user-service"))  // lb:// = LoadBalanced
            .build();
    }
}

// 실제 동작:
// 1. "lb://user-service" 확인
// 2. Eureka 캐시에서 user-service 인스턴스 목록 조회
// 3. LoadBalancer가 하나 선택 (Round Robin, Random, etc.)
// 4. 선택된 인스턴스로 직접 HTTP 요청
```

### Eureka 캐싱 설정

```yaml
# Gateway의 application.yml
eureka:
  client:
    registry-fetch-interval-seconds: 30  # 30초마다 갱신
    cache-refresh-enabled: true
    initial-instance-info-replication-interval-seconds: 40
    
  instance:
    lease-renewal-interval-in-seconds: 30  # Heartbeat 주기
    lease-expiration-duration-in-seconds: 90  # 90초 응답 없으면 제거
```

### 성능 비교

| 방식 | 요청당 Eureka 조회 | Eureka 부하 (1000 TPS) | 응답 시간 | 장애 허용성 |
|------|-------------------|----------------------|----------|------------|
| 매번 조회 | ✅ 매 요청마다 | 1000 TPS | 110ms | ❌ 낮음 |
| 캐싱 (베스트) | ❌ 30초마다 1번 | 0.033 TPS | 100ms | ✅ 높음 |

### 추가 이점

#### 1. Circuit Breaker 적용 가능
```java
.route("user-service", r -> r.path("/user/**")
    .filters(f -> f
        .circuitBreaker(c -> c
            .setName("userServiceCB")
            .setFallbackUri("/fallback/user"))
    )
    .uri("lb://user-service"))
```

#### 2. Canary 배포 지원
```yaml
routes:
  - id: user-v1
    uri: lb://user-service-v1
    predicates:
      - Weight=user-group, 90  # 90% 트래픽
      
  - id: user-v2
    uri: lb://user-service-v2
    predicates:
      - Weight=user-group, 10  # 10% 트래픽 (신규 버전 테스트)
```

#### 3. Retry 및 Timeout 설정
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/user/**
          filters:
            - name: Retry
              args:
                retries: 3
                statuses: BAD_GATEWAY
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10
```

---

## 동시성 및 충돌 방지 메커니즘

### 🎯 결론: 충돌 없이 동시 처리 가능!

우리의 마이크로서비스 아키텍처는 **수천 개의 동시 요청을 충돌 없이 처리**할 수 있습니다.

### 왜 충돌이 나지 않는가?

#### 1. Stateless 아키텍처 🔄
```
각 요청은 완전히 독립적:

요청 A: Frontend → Gateway → User Service (8082)
요청 B: Frontend → Gateway → Diary Service (8083)
요청 C: Frontend → Gateway → User Service (8082)

→ 각 요청이 독립된 스레드에서 처리
→ 공유 상태 없음 (Stateless)
→ 동시 처리 가능 ✅
```

#### 2. 독립적인 서비스 포트 🚪
```yaml
각 서비스는 자신만의 포트 사용:
- user-service: 8082
- diary-service: 8083
- calendar-service: 8084
- soccer-service: 8085
- culture-service: 8086
- healthcare-service: 8087

→ 물리적으로 분리됨
→ 절대 충돌 불가능 ✅
```

#### 3. Gateway의 Non-blocking I/O (WebFlux) ⚡
```
전통적 방식 (Blocking):
요청 1 → Thread 1 (대기 중...)
요청 2 → Thread 2 (대기 중...)
...
요청 200 → Thread 200 (대기 중...)
요청 201 → ❌ Thread 부족! 대기...

Gateway (Non-blocking WebFlux):
요청 1-10000 → Event Loop (스레드 4개)
→ 모두 비동기로 처리
→ 동시 수만 개 처리 가능 ✅
```

#### 4. 라우팅 경로 분리 🛣️
```yaml
# Gateway 라우팅 설정
spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/user/**      ← /user로 시작
            
        - id: diary-service
          uri: lb://diary-service
          predicates:
            - Path=/diary/**     ← /diary로 시작

→ 경로가 겹치지 않음
→ 라우팅 충돌 불가능 ✅
```

### 동시 요청 처리 시뮬레이션

```
시간 T=0ms: 1000개 동시 요청 도착

[1000 Requests]
    ↓ (모두 동시에)
┌────────────────┐
│   Gateway      │ ← Non-blocking WebFlux
│  (4 threads)   │   Event Loop로 모두 수용
└────┬───────────┘
     │ 라우팅 (경로별 분산)
     │
┌────┼───────┬──────┬──────┬──────┐
│    │       │      │      │      │
▼    ▼       ▼      ▼      ▼      ▼
User  Diary  Cal   Soccer Culture Health
8082  8083   8084  8085   8086    8087

각 서비스:
- 독립된 프로세스
- 독립된 메모리 공간
- 독립된 스레드 풀
- 독립된 DB 커넥션 풀

→ 서로 간섭 불가능! ✅
```

### 충돌 방지 메커니즘 상세

#### 1. 커넥션 풀 관리
```yaml
# 각 서비스의 PostgreSQL 설정
spring:
  datasource:
    hikari:
      maximum-pool-size: 10      # 동시 10개 연결
      minimum-idle: 5
      connection-timeout: 30000  # 30초 대기
      max-lifetime: 1800000      # 30분

→ 동시 요청 100개여도:
  - 10개씩 처리
  - 나머지는 큐에서 대기
  - 안전하게 순차 처리 ✅
```

#### 2. Redis 커넥션 풀
```yaml
spring:
  redis:
    lettuce:
      pool:
        max-active: 8    # 동시 8개 연결
        max-idle: 8
        min-idle: 0

→ Redis 접근도 안전하게 관리 ✅
```

#### 3. 스레드 풀 관리
```yaml
# Spring MVC 기본 설정 (Tomcat)
server:
  tomcat:
    threads:
      max: 200          # 최대 200개 스레드
      min-spare: 10     # 최소 10개 대기

→ 동시 200개 요청까지 처리
→ 초과 시 큐에서 대기 ✅
```

### 동시성 처리 능력 계산

```
현재 구조의 이론적 처리 능력:

Gateway (WebFlux):
└─ 단일 인스턴스: ~10,000 TPS
   (Non-blocking이므로 CPU 바운드)

User Service (Spring MVC):
└─ 단일 인스턴스: ~500 TPS
   (스레드 풀 200개 기준, 응답시간 400ms 가정)

시스템 전체:
├─ User Service (3 인스턴스): 1,500 TPS
├─ Diary Service (2 인스턴스): 1,000 TPS
├─ Calendar Service (2 인스턴스): 1,000 TPS
├─ Soccer Service (2 인스턴스): 1,000 TPS
└─ 합계: ~4,500 TPS

→ 동시 요청 4,500개까지 충돌 없이 처리 가능! 🚀
```

### 부하 테스트 예상 결과

```bash
# Apache Bench로 1000개 동시 요청
ab -n 1000 -c 100 http://localhost:8080/user/123

# 예상 결과:
Concurrency Level:      100
Time taken for tests:   0.667 seconds
Complete requests:      1000
Failed requests:        0           ← 충돌 없음!
Requests per second:    1500.00 [#/sec]
Time per request:       66.67 [ms]
```

### 충돌이 날 수 있는 경우 (우리는 안전함)

#### ❌ 안티패턴 1: 공유 상태
```java
// 나쁜 예 (우리는 안 함!)
public class UserService {
    private static int requestCount = 0;  // 공유 변수!
    
    public User getUser(Long id) {
        requestCount++;  // Race Condition 발생 💥
        return userRepository.findById(id);
    }
}
```

#### ✅ 우리 방식: Stateless
```java
// 좋은 예 (우리가 사용)
@Service
public class UserService {
    @Autowired
    private UserRepository userRepository;  // 스레드 안전
    
    public User getUser(Long id) {
        return userRepository.findById(id);  // 충돌 없음! ✅
    }
}
```

#### ❌ 안티패턴 2: 같은 포트 사용
```yaml
# 나쁜 예 (우리는 안 함!)
user-service:
  ports: ["8080:8080"]
diary-service:
  ports: ["8080:8080"]  # 💥 포트 충돌!
```

#### ✅ 우리 방식: 포트 분리
```yaml
# 좋은 예 (docker-compose.yaml)
user-service:
  ports: ["8082:8082"]
diary-service:
  ports: ["8083:8083"]  # ✅ 각자 다른 포트
```

### 동시성 보장 원칙

```
1. Stateless 설계
   → 서비스가 상태를 저장하지 않음
   → 모든 상태는 DB/Redis에만 저장

2. Immutable Data
   → 요청 데이터는 변경 불가능
   → 새로운 객체 생성으로 처리

3. Thread-Safe Components
   → Spring Bean은 기본적으로 싱글톤이지만 스레드 안전
   → 인스턴스 변수 사용 최소화

4. Connection Pooling
   → DB, Redis 커넥션을 풀로 관리
   → 동시 접근 제어

5. 독립적인 프로세스
   → 각 마이크로서비스는 독립 실행
   → 메모리 공간 분리
```

### 실제 동시 처리 예시

```
동시에 1000명이 다른 서비스 호출:

T=0ms:
├─ User A: GET /user/1
├─ User B: GET /diary/100
├─ User C: GET /calendar/50
├─ User D: GET /user/2
└─ ... (996 more)

Gateway:
├─ Event Loop로 모두 수용 (Non-blocking)
├─ 라우팅:
│   ├─ 300개 → user-service
│   ├─ 250개 → diary-service
│   ├─ 200개 → calendar-service
│   └─ 250개 → 기타 서비스
└─ 모두 동시에 전달

각 서비스:
├─ 스레드 풀로 순차 처리
├─ DB 커넥션 풀로 안전하게 접근
└─ 완료 후 Gateway로 응답

Gateway:
└─ 각 사용자에게 응답 전달

결과:
└─ 충돌 0개, 실패 0개 ✅
```

### 성능 최적화 전략

```yaml
# 1. Redis 캐싱으로 DB 부하 감소
@Cacheable(value = "users", key = "#id")
public User findUser(Long id) { ... }

# 2. 비동기 처리 (필요 시)
@Async
public CompletableFuture<User> findUserAsync(Long id) { ... }

# 3. Rate Limiting (Gateway)
spring:
  cloud:
    gateway:
      routes:
        - filters:
            - name: RequestRateLimiter
              args:
                redis-rate-limiter.replenishRate: 10

# 4. Circuit Breaker (장애 격리)
@CircuitBreaker(name = "userService")
public User findUser(Long id) { ... }
```

---

## 실제 요청 시나리오

### 시나리오 1: 사용자 정보 조회

```
[Frontend]
  ↓ GET http://localhost:8080/user/123
  
[Gateway:8080]
  ↓ 1. 라우팅 규칙 확인: /user/** → user-service
  ↓ 2. Eureka 조회: "user-service 어디?"
  ↓ 3. Eureka 응답: "8082 포트"
  ↓ 4. 요청 전달: http://user-service:8082/user/123
  
[User Service:8082]
  ↓ 1. Redis 캐시 확인
  │    - Key: "user:123"
  │    - 있으면 바로 반환 (캐시 히트)
  │    - 없으면 DB 조회
  ↓ 2. PostgreSQL 조회
  │    - SELECT * FROM users WHERE id = 123
  ↓ 3. Redis에 캐싱
  │    - SET user:123 {data} EX 300 (5분)
  ↓ 4. JSON 응답 생성
  │    {
  │      "code": 200,
  │      "message": "Success",
  │      "data": {
  │        "id": 123,
  │        "name": "홍길동",
  │        "email": "hong@example.com"
  │      }
  │    }
  ↓ 5. HTTP 응답 반환
  │    Status: 200 OK
  │    Content-Type: application/json
  │    Body: {JSON}
  
[Gateway:8080]
  ↓ 1. User Service로부터 응답 수신
  ↓ 2. 응답 검증
  │    - Status Code 확인
  │    - CORS 헤더 추가 (필요 시)
  ↓ 3. Frontend로 응답 전달
  │    Status: 200 OK
  │    Headers: 
  │      - Content-Type: application/json
  │      - Access-Control-Allow-Origin: *
  │    Body: {JSON}
  
[Frontend - API Client]
  ↓ 1. fetchWithRetry()로 응답 수신
  ↓ 2. 응답 상태 확인
  │    - response.ok 체크
  │    - response.status 확인
  ↓ 3. 에러 처리
  │    - 5xx 에러 → 자동 재시도 (최대 2회)
  │    - 네트워크 에러 → 자동 재시도
  │    - 타임아웃 (30초) → AbortError
  ↓ 4. JSON 파싱
  │    const result = await response.json()
  │    {
  │      code: 200,
  │      message: "Success",
  │      data: { ... }
  │    }
  
[Frontend - React Query / 직접 처리]
  ↓ 방법 1: React Query 사용
  │  const { data, isLoading, error } = useQuery({
  │    queryKey: ['user', 123],
  │    queryFn: async () => {
  │      const response = await fetchFromGateway('/user/123');
  │      const result = await response.json();
  │      if (result.code === 200) return result.data;
  │      throw new Error(result.message);
  │    }
  │  });
  │  ↓
  │  - 자동 캐싱 (staleTime: 1분)
  │  - 백그라운드 동기화
  │  - 에러 상태 관리
  │
  ↓ 방법 2: 직접 fetch 사용
  │  const response = await fetchFromGateway('/user/123');
  │  const result = await response.json();
  │  if (result.code === 200 && result.data) {
  │    // Zustand 스토어에 저장
  │    setUserData(result.data);
  │  }
  
[Frontend - 상태 업데이트]
  ↓ React Query 사용 시:
  │  - QueryClient 캐시에 자동 저장
  │  - 컴포넌트 자동 리렌더링
  │
  ↓ Zustand 사용 시:
  │  - useAppStore.getState().setUserData(result.data)
  │  - 구독 중인 컴포넌트 자동 업데이트
  
[Frontend - UI 렌더링]
  ↓ 컴포넌트 리렌더링
  │  - React Query: data, isLoading, error 상태로 UI 표시
  │  - Zustand: 스토어 구독으로 자동 업데이트
  ↓ 사용자 정보 표시
  │  ✓ 이름: 홍길동
  │  ✓ 이메일: hong@example.com
```

### 시나리오 2: AI 감정 분석 (예정)

```
[Frontend]
  ↓ POST http://localhost:8080/ai/analyze
  ↓ Body: {"text": "오늘 기분이 좋아요"}
  
[Gateway:8080]
  ↓ 1. 라우팅: /ai/** → ai-middleware
  ↓ 2. Eureka 조회 (또는 직접 주소 사용)
  ↓ 3. 요청 전달: http://ai-middleware:8090/analyze
  
[AI Middleware:8090]
  ↓ 1. Redis 큐에 작업 추가 (비동기 처리용)
  │    - LPUSH ai:queue {"text": "오늘 기분이 좋아요"}
  ↓ 2. Worker가 큐에서 작업 가져옴
  │    - RPOP ai:queue
  ↓ 3. AI 모델 추론
  │    - 감정 분석 모델 실행
  │    - 결과: {"sentiment": "positive", "score": 0.95}
  ↓ 4. Redis 캐싱
  │    - SET analysis:hash... {result} EX 3600
  ↓ 5. 응답 반환
  
[Gateway:8080]
  ↓ 응답 전달
  
[Frontend]
  ✓ "긍정적인 감정입니다!" 표시
```

### 시나리오 3: 서비스 간 통신 (Pub/Sub)

```
[Diary Service:8083]
  ↓ 사용자가 새 일기 작성
  ↓ 1. PostgreSQL에 저장
  │    - INSERT INTO diaries ...
  ↓ 2. Redis Pub/Sub 이벤트 발행
  │    - PUBLISH diary.created {"diaryId": 456, "userId": 123}
  
[Redis:6379]
  ↓ 이벤트 브로드캐스트
  
[Calendar Service:8084] (구독자)
  ↓ 이벤트 수신
  ↓ 자동으로 일정에 일기 작성 이벤트 추가
  
[User Service:8082] (구독자)
  ↓ 이벤트 수신
  ↓ 사용자 활동 통계 업데이트
  
[AI Middleware:8090] (구독자)
  ↓ 이벤트 수신
  ↓ 일기 내용 감정 분석 (비동기)
```

---

## 응답 처리 흐름 (Response Flow)

### 전체 응답 흐름 개요

```
[Microservice] → [Gateway] → [Frontend API Client] → [상태 관리] → [UI]
     JSON           HTTP          Response 객체        캐시/스토어    렌더링
```

### 1. 백엔드 응답 생성 (Microservice)

#### 성공 응답
```json
HTTP/1.1 200 OK
Content-Type: application/json

{
  "code": 200,
  "message": "Success",
  "data": {
    "id": 123,
    "name": "홍길동",
    ...
  }
}
```

#### 에러 응답
```json
HTTP/1.1 400 Bad Request
Content-Type: application/json

{
  "code": 400,
  "message": "Invalid request parameter",
  "data": null
}
```

#### 서버 에러 응답
```json
HTTP/1.1 500 Internal Server Error
Content-Type: application/json

{
  "code": 500,
  "message": "Internal server error",
  "data": null
}
```

### 2. Gateway 응답 처리

#### Gateway의 역할
1. **응답 수신**: Microservice로부터 HTTP 응답 받음
2. **CORS 헤더 추가**: 브라우저 CORS 정책 준수
3. **에러 변환**: 필요 시 에러 응답 포맷 통일
4. **로깅**: 응답 로그 기록 (선택적)

#### Gateway 응답 흐름
```
[User Service 응답]
  ↓ HTTP 200 OK + JSON
[Gateway]
  ↓ 1. 응답 수신
  ↓ 2. CORS 헤더 추가
  │    Access-Control-Allow-Origin: *
  │    Access-Control-Allow-Methods: GET, POST, PUT, DELETE
  │    Access-Control-Allow-Headers: Content-Type, Authorization
  ↓ 3. 응답 전달 (변경 없이 그대로)
  ↓ HTTP 200 OK + JSON + CORS Headers
[Frontend]
```

### 3. Frontend API Client 응답 처리

#### fetchWithRetry() 응답 처리

```typescript
// 1. 응답 수신
const response = await fetch(url, options);

// 2. 상태 코드 확인
if (!response.ok && response.status >= 500) {
  // 5xx 에러 → 자동 재시도
  if (retries > 0) {
    await delay(1000);
    return fetchWithRetry(url, options, retries - 1);
  }
}

// 3. 타임아웃 처리
const controller = new AbortController();
const timeoutId = setTimeout(() => controller.abort(), 30000);
// 타임아웃 시 AbortError 발생 → 재시도

// 4. 네트워크 에러 처리
catch (error) {
  if (error.name === 'AbortError' || 
      error.message.includes('network')) {
    // 재시도
  }
  throw error; // 최종 실패
}
```

#### 응답 상태별 처리

| 상태 코드 | 처리 방식 | 재시도 |
|----------|----------|--------|
| 200-299 | 정상 처리 | ❌ |
| 400-499 | 클라이언트 에러 | ❌ (재시도 불필요) |
| 500-599 | 서버 에러 | ✅ (최대 2회) |
| 네트워크 에러 | 네트워크 문제 | ✅ (최대 2회) |
| 타임아웃 | 30초 초과 | ✅ (최대 2회) |

### 4. JSON 파싱 및 검증

```typescript
// API Client에서 응답 받음
const response = await fetchFromGateway('/user/123');

// JSON 파싱
const result = await response.json();
// {
//   code: 200,
//   message: "Success",
//   data: { ... }
// }

// 응답 코드 검증
if (result.code === 200 && result.data) {
  // 성공: 데이터 사용
  return result.data;
} else {
  // 실패: 에러 처리
  throw new Error(result.message || 'Unknown error');
}
```

### 5. 상태 관리 계층별 응답 처리

#### React Query 사용 시

```typescript
const { data, isLoading, error } = useQuery({
  queryKey: ['user', userId],
  queryFn: async () => {
    // 1. API 호출
    const response = await fetchFromGateway(`/user/${userId}`);
    
    // 2. JSON 파싱
    const result = await response.json();
    
    // 3. 에러 체크
    if (result.code !== 200) {
      throw new Error(result.message);
    }
    
    // 4. 데이터 반환 (자동 캐싱됨)
    return result.data;
  },
  staleTime: 60 * 1000, // 1분간 캐시 유지
  retry: 2, // React Query 자체 재시도
});

// React Query가 자동으로:
// - 캐싱: result.data를 QueryClient 캐시에 저장
// - 동기화: 백그라운드에서 자동 refetch
// - 상태 관리: isLoading, error 상태 제공
// - 리렌더링: data 변경 시 컴포넌트 자동 업데이트
```

#### Zustand 사용 시

```typescript
// 직접 fetch 사용
const response = await fetchFromGateway('/user/123');
const result = await response.json();

if (result.code === 200 && result.data) {
  // Zustand 스토어에 저장
  useAppStore.getState().setUserData(result.data);
  // 또는
  useAppStore.setState({ user: result.data });
}

// 컴포넌트에서 구독
const user = useAppStore((state) => state.user);
// user 변경 시 자동 리렌더링
```

### 6. 에러 처리 흐름

#### 에러 발생 시나리오

```
[에러 발생]
  ↓
[API Client - fetchWithRetry]
  ↓ 1차 재시도 (1초 대기)
  ↓ 실패
  ↓ 2차 재시도 (1초 대기)
  ↓ 실패
  ↓ 최종 에러 throw
  ↓
[React Query / 직접 처리]
  ↓ catch (error)
  ↓ 에러 상태 설정
  │  - React Query: error 상태 자동 설정
  │  - Zustand: setError(error.message)
  ↓
[UI 렌더링]
  ↓ 에러 메시지 표시
  │  - React Query: error.message
  │  - Zustand: state.error
```

#### 에러 타입별 처리

| 에러 타입 | 원인 | 처리 방식 |
|----------|------|----------|
| 400 Bad Request | 잘못된 요청 | 사용자에게 입력 오류 안내 |
| 401 Unauthorized | 인증 실패 | 로그인 페이지로 리다이렉트 |
| 403 Forbidden | 권한 없음 | 권한 필요 메시지 표시 |
| 404 Not Found | 리소스 없음 | 404 페이지 또는 "데이터 없음" 표시 |
| 500 Internal Server Error | 서버 에러 | 재시도 후 실패 시 "서버 오류" 메시지 |
| Network Error | 네트워크 문제 | "연결 실패" 메시지 + 재시도 버튼 |
| Timeout | 타임아웃 | "요청 시간 초과" 메시지 + 재시도 버튼 |

### 7. 캐싱 및 동기화

#### React Query 캐싱

```
[첫 요청]
  GET /user/123
  ↓
  응답: { id: 123, name: "홍길동" }
  ↓
  QueryClient 캐시에 저장
  │  Key: ['user', 123]
  │  Value: { id: 123, name: "홍길동" }
  │  staleTime: 60초

[같은 데이터 재요청 (60초 이내)]
  ↓ 캐시에서 즉시 반환 (네트워크 요청 없음)
  ↓ UI 즉시 업데이트

[60초 후]
  ↓ 캐시가 stale 상태
  ↓ 백그라운드에서 자동 refetch
  ↓ 새 데이터로 캐시 업데이트
  ↓ UI 자동 업데이트
```

#### Zustand + 수동 캐싱

```typescript
// 수동으로 캐시 관리
const cache = new Map();

async function fetchUser(id: number) {
  // 캐시 확인
  if (cache.has(id)) {
    const cached = cache.get(id);
    if (Date.now() - cached.timestamp < 60000) {
      return cached.data; // 캐시 히트
    }
  }
  
  // API 호출
  const response = await fetchFromGateway(`/user/${id}`);
  const result = await response.json();
  
  // 캐시 저장
  cache.set(id, {
    data: result.data,
    timestamp: Date.now()
  });
  
  return result.data;
}
```

### 8. UI 업데이트 흐름

#### React Query 사용 시

```typescript
function UserProfile({ userId }: { userId: number }) {
  const { data, isLoading, error } = useQuery({
    queryKey: ['user', userId],
    queryFn: () => fetchUser(userId)
  });
  
  // 자동 리렌더링:
  // - isLoading: true → false (로딩 스피너 표시)
  // - error: null → Error (에러 메시지 표시)
  // - data: undefined → UserData (사용자 정보 표시)
  
  if (isLoading) return <Spinner />;
  if (error) return <ErrorMessage error={error} />;
  return <UserInfo data={data} />;
}
```

#### Zustand 사용 시

```typescript
function UserProfile({ userId }: { userId: number }) {
  const user = useAppStore((state) => state.user);
  const isLoading = useAppStore((state) => state.isLoading);
  const error = useAppStore((state) => state.error);
  
  // 스토어 변경 시 자동 리렌더링
  // - user 변경 → UserInfo 업데이트
  // - isLoading 변경 → Spinner 표시/숨김
  // - error 변경 → ErrorMessage 표시
  
  useEffect(() => {
    fetchUser(userId); // API 호출
  }, [userId]);
  
  if (isLoading) return <Spinner />;
  if (error) return <ErrorMessage error={error} />;
  return <UserInfo data={user} />;
}
```

### 응답 처리 요약

```
1. Microservice → JSON 응답 생성
2. Gateway → CORS 헤더 추가 + 응답 전달
3. API Client → fetchWithRetry로 응답 수신 + 재시도 처리
4. JSON 파싱 → result.code, result.data 검증
5. 상태 관리:
   - React Query: 자동 캐싱 + 동기화
   - Zustand: 수동 스토어 업데이트
6. UI 업데이트:
   - 로딩 상태 → 데이터 표시
   - 에러 상태 → 에러 메시지 표시
```

---

## 포트 맵

### Infrastructure
| 서비스 | 포트 | 용도 | 외부 접근 |
|--------|------|------|-----------|
| Eureka | 8761 | Service Registry | ✅ (대시보드) |
| Config Server | 8888 | 중앙 설정 관리 | ❌ |

### Edge Layer
| 서비스 | 포트 | 용도 | 외부 접근 |
|--------|------|------|-----------|
| Gateway | 8080 | API Gateway | ✅ (모든 API) |

### Application Services
| 서비스 | 포트 | 용도 | 외부 접근 |
|--------|------|------|-----------|
| common-service | 8081 | 공통 기능 | ❌ (Gateway 경유) |
| user-service | 8082 | 사용자 관리 | ❌ (Gateway 경유) |
| diary-service | 8083 | 일기 관리 | ❌ (Gateway 경유) |
| calendar-service | 8084 | 일정 관리 | ❌ (Gateway 경유) |
| soccer-service | 8085 | 축구 정보 | ❌ (Gateway 경유) |
| culture-service | 8086 | 문화 정보 | ❌ (Gateway 경유) |
| healthcare-service | 8087 | 건강 관리 | ❌ (Gateway 경유) |
| ai-middleware | 8090 | AI 추론 | ❌ (Gateway 경유) |

### Data Layer
| 서비스 | 포트 | 용도 | 외부 접근 |
|--------|------|------|-----------|
| PostgreSQL | 5432 | 메인 DB | ❌ (서비스만) |
| Redis | 6379 | 캐시/세션/큐 | ❌ (서비스만) |

### Client
| 서비스 | 포트 | 용도 | 외부 접근 |
|--------|------|------|-----------|
| Frontend | 3000 | Next.js | ✅ (사용자) |

---

## 핵심 정리

### ✅ 맞는 이해
1. **Gateway는 독립적인 서비스**
   - Eureka, Config Server와 같은 계층이 아님
   - Eureka의 "클라이언트" (서비스 조회용)

2. **Eureka는 "전화번호부"**
   - 모든 서비스가 자신을 등록
   - Gateway가 서비스 주소를 조회

3. **모든 요청은 Gateway를 통함**
   - Frontend → Gateway → Microservice

4. **서비스들은 서로 독립적**
   - 각자 Eureka에 등록
   - 필요 시 Gateway 없이 직접 통신 가능 (권장하지 않음)

### ❌ 잘못된 이해
1. Gateway가 Eureka/Config "안에" 있다 → ❌
2. Gateway가 Eureka "사이에" 있다 → ❌
3. Gateway가 없으면 서비스 간 통신 불가 → ❌ (하지만 Gateway 사용 권장)

---

## 다음 단계

### Phase 1: 현재 구조 안정화
- [ ] Redis 연동 테스트
- [ ] Gateway Rate Limiting 구현
- [ ] 각 서비스 캐싱 적용

### Phase 2: AI Middleware 구축
- [ ] FastAPI 기반 AI 서비스 생성
- [ ] Eureka 등록 또는 Gateway 직접 라우팅
- [ ] Redis 큐 기반 비동기 처리

### Phase 3: Observability
- [ ] Prometheus + Grafana (모니터링)
- [ ] Zipkin (분산 추적)
- [ ] ELK Stack (로그 수집)

### Phase 4: 고급 기능
- [ ] Circuit Breaker (Resilience4j)
- [ ] Outbox 패턴 (이벤트 소싱)
- [ ] CQRS 패턴 적용

---

**작성일**: 2024-11-21  
**최종 수정일**: 2024-12-19  
**버전**: 2.2  
**프로젝트**: AIION Microservices Architecture

### 변경 이력
- **v2.2** (2024-12-19)
  - 응답 처리 흐름 (Response Flow) 섹션 추가
  - 시나리오 1의 응답 처리 과정 상세화
  - 백엔드 → Gateway → Frontend 응답 흐름 상세 설명
  - React Query 및 Zustand의 응답 처리 방식 비교
  - 에러 처리 및 재시도 로직 설명
  - 캐싱 및 동기화 메커니즘 설명
  - UI 업데이트 흐름 설명
- **v2.1** (2024-12-19)
  - 프론트엔드 아키텍처 상세화
  - React Query (@tanstack/react-query) 추가 반영
  - 프론트엔드 내부 구조 다이어그램 추가 (Atomic Design, 상태 관리 계층)
  - Zustand 슬라이스 구조 및 React Query 통합 설명 추가
  - API Client 구조 및 데이터 흐름 설명 추가
- **v2.0** (2024-11-21)
  - Gateway와 Eureka 통신 메커니즘 추가 (베스트 프랙티스 설명)
  - 동시성 및 충돌 방지 메커니즘 추가
  - 성능 최적화 전략 추가
- **v1.0** (2024-11-21)
  - 초기 버전 작성

